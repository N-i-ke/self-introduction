# Node.js modules
node_modules/

# macOS system files
.DS_Store

# Build output (deployed via GitHub Actions, not committed)
docs/

# Vite local build cache
.vite/

# Logs
*.log

# Vite env (local overrides only — .env / .env.production はコミット対象)
.env.local
.env.*.local
